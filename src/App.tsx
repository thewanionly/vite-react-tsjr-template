import styled from 'styled-components'

const S = {
  App: styled.div``,
  AppTitle: styled.h1``,
}

export const App = () => {
  return (
    <S.App>
      <S.AppTitle>Vite + React + TSJR Template</S.AppTitle>
    </S.App>
  )
}
